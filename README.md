# Santini-Gateway
 A unified Apollo Federation GraphQL gateway with an operator dashboard and protected administration API for registering downstream subgraphs, validating their endpoints, generating runtime gateway configuration, routing federated client queries, and recording configuration changes; non-goals are subgraph code generation, automatic schema-breaking.
